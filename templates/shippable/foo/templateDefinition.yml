pipelines:
  - name: basic
    steps:
      - name: basic1
        type: Bash
        configuration:
          runtime:
            type: host
        execution:
          onExecute:
            - echo "here 
      - name: basic2
        type: Bash
        configuration:
          runtime:
            type: host
          inputSteps:
            - name: basic1
        execution:
          onExecute:
            - echo "here"
